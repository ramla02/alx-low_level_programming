#include "main.h"
/**
 * _isdigit - a function that checks for a digit (0 through 9).
 * @c: integer
 * Returns 1 if c is a digit
 * Return: 0 otherwise
 */
int _isdigit(int c)
{

	if (c <= 'a')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
